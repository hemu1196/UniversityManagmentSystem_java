package com.university;

import com.university.ui.LoginUI;

public class Main {
    public static void main(String[] args) {
        // Initialize and start the Login loop
        LoginUI loginUI = new LoginUI();
        loginUI.start();
    }
}
